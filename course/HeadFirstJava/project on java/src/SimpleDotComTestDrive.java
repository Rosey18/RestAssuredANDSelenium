/*public class SimpleDotComTestDrive {
    public static void main(String[] args) {
        DotCom dot = new DotCom(); //создаем экземпляр класса DotCom
        int[] locations = {2, 3, 4}; //создаем массив для местоположения "сайта"
        dot.setLocationCells(locations); //вызываем сеттер сайта //locations - это аргумент он передает массив местоположения "сайта" в параметр метода сеттр а именно в locs который в свою очередь передает значение в locationCells

        String userGuess = "4"; //делаем ход от имени пользователя
        String result = dot.checkYourSelf(userGuess); //вызываем метод checkYourSelf объекта DotCom //userGuess является аргументом, и передает ход пользоваетля в метод checkYourSelf а именно в stringGuess
        String testResult = "Unlucky";
        if (result.equals("Yep!")) {
            testResult = "Pass"; //если ход (2) возвращает строку "Yep", значит все работает
        }
        System.out.println(testResult); //напечатает результат прохождения теста (Pass, Unlucky)
    }
}
*/
                                //ТЕСТОВЫЙ КОД//